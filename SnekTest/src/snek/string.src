

externc func strncmp(char* str1, char* str2, uint64 n) int;


func substring(string str, int offset, int count)
	=> string(str.data + offset, count);

func startsWith(string str, string value)
	=> str.length >= value.length && strncmp(str.data, value.data, value.length) == 0;
func endsWith(string str, string value)
	=> str.length >= value.length && strncmp(str.data + str.length - value.length, value.data, value.length) == 0;
