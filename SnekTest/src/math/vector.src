


struct vec2 {
	float x, y;
}

struct vec3 {
	float x, y, z;
	vec2 xy @offset(0);
	vec2 yz @offset(4);
}

struct vec4 {
	float x, y, z, w;
	vec2 xy @offset(0);
	vec2 yz @offset(4);
	vec2 zw @offset(8);
	vec3 xyz @offset(0);
	vec3 yzw @offset(4);
}